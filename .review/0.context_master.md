# 📘 CONTEXTO MAESTRO: ALGARROBO_BASE (Módulo Seguridad)

## 📋 METADATA
- **Fecha**: 2026-03-03T16:05:00Z
- **Stack**: Vanilla JS (ES2020+) / Tailwind CSS / Web APIs
- **Browser Support**: Modern only (vía DecompressionStream & ES2020)
- **Criticidad**: ALTA (Dashboard de Seguridad Pública / Toma de Decisiones)

## 🎯 OBJETIVOS DE NEGOCIO
Proporcionar una herramienta visual y analítica para la gestión de seguridad municipal, integrando datos oficiales de Carabineros de Chile (STOP) y el Ministerio del Interior (CEAD). El sistema permite la generación de reportes estratégicos (PDF) y el análisis de inteligencia criminal asistido por IA (GLM-4) para optimizar la focalización de recursos preventivos.

## 🏗️ ARQUITECTURA TÉCNICA
- **Patrón**: Singleton / Module Pattern con orquestación vía eventos globales (`dataManagerLoaded`, `viewLoaded`).
- **Flujo de Datos**:
    1. Carga de datos comprimidos (Gzip) vía Fetch API.
    2. Descompresión en cliente vía `DecompressionStream`.
    3. Normalización y enriquecimiento de taxonomía en `DataManager`.
    4. Renderizado dinámico de vistas HTML (vistas2) en un contenedor central.
    5. Inyección de gráficos (Chart.js) y análisis de IA (ia2.js).
- **Web Standards Compliance**: Alto uso de estándares modernos (Async/Await, CustomEvents, Fetch, Storage).
- **Restricciones de Entorno**: 
    - Offline-first: Implementado vía `localStorage` con TTL de 2 horas.
    - Dependencias Externas: Fuerte dependencia de CDNs (Tailwind, Chart.js, BigModel API).
    - CSP: Debe permitir conexiones a `open.bigmodel.cn` y CDNs de Cloudflare/JSDelivr.

## ⚠️ RESTRICCIONES Y ÁREAS SENSIBLES
- **Core de Datos (`js/data_manager.js`)**: No modificar la lógica de descompresión o normalización del "legacy state", ya que las vistas dependen de variables globales (`window.STATE_DATA`).
- **Seguridad de API Keys (`js/ia2.js`)**: La clave de BigModel está ofuscada pero expuesta en el cliente. Requiere revisión de mitigación.
- **Manipulación de DOM (`js/app.js`)**: El mecanismo de "Monkey Patch" y sobreescritura de métodos del objeto global `App` es frágil.
- **Límites de Cuota**: `localStorage` es propenso a `QuotaExceededError` debido al almacenamiento de análisis de IA.

## 🎯 INSTRUCCIONES ESPECÍFICAS PARA EL PIPELINE
- **A error-watcher**: "Enfócate en la gestión de errores de `fetch` y `DecompressionStream`. Valida la robustez de los `setTimeout` en la sincronización de IA."
- **A compliance-reviewer**: "Prioriza la falta de SRI (Subresource Integrity) en los scripts de CDN y valida el riesgo de XSS en la inyección de `innerHTML` de las vistas dinámicas."
- **A code-reviewer**: "Validar el 'DOM Thrashing' durante la carga de gráficos y la consistencia del Singleton de `DataManager`."

---
*Documento generado automáticamente por Antigravity Professional.*
